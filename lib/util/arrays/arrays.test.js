var assert = require('assert');

describe("Arrays", function() {
  it("Arrays remove ", function() {
    var Arrays = require('./arrays').Arrays;
var arrs = new Arrays();

    arrs.remove();
   
  });
});
